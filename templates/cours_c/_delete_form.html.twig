<form method="post" action="{{ path('cours_c_delete', {'id': cour.Idcours}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input hidden name="_token" value="{{ csrf_token('delete' ~ cour.Idcours) }}">
    <button type="submit" class="btn btn-gradient-danger btn-rounded">Delete</button>
</form>


