<form method="post" action="{{ path('commentaire_delete', {'idCommentaire': commentaire.idCommentaire}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commentaire.idCommentaire) }}">
    <button  type="submit" class="btn btn-gradient-danger btn-rounded ">Delete</button>
</form>
