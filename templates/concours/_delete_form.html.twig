<form method="post" action="{{ path('concours_delete', {'idConcours': concour.idConcours}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ concour.idConcours) }}">
    <button  type="submit" class="btn btn-gradient-danger btn-rounded btn-fw">Delete</button>
</form>
